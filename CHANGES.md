# V6 修复内容

## 1. Prompt 压缩

- 模型只生成 `detailed_conclusion`、`causes`、`safety_impact`；
- `treatments` 始终保留确定性 Baseline；
- 病害按构件和类型聚合，最多 12 组，每组只保留 1 条短描述；
- 建议最多 8 条，只提供必要短文本；
- 当前报告证据、任务检索结果和查询分别设置小预算；
- 不再重复传入完整 Baseline、完整锁定事实和多份相同检索内容。

Review 包 16 份既有产物的同口径 Prompt 字符数：

```text
平均：53,870 → 15,741（下降 70.78%）
中位数：57,782 → 16,123
最大值：63,372 → 16,825
```

字符数不等同于模型 Token 数，真实 Token 需以复跑调用指标为准。

## 2. 修复任务级检索丢失

原批跑脚本先为每个字段分别检索，但进入 LangGraph 前又合并成一份命中，导致详细结论、成因和安全影响实际收到相同证据。

V6 改为：

```text
详细结论查询 → 详细结论命中
成因查询       → 成因命中
安全影响查询   → 安全影响命中
```

合并命中仅用于汇总展示，不再作为三个任务的共同输入。

## 3. 安全影响校准

- 每条安全影响必须引用当前报告的病害事实或安全评估证据；
- 存在安全评估原文时，至少一条必须引用该证据；
- “影响较小、满足要求、良好状态”不得被扩写成重大风险；
- 知识卡和相似标签不能单独支撑当前样本的安全结论；
- 无效证据、无报告病害依据或新增构件时，该字段直接回退。

## 4. 设施与构件约束

- Prompt 只暴露当前报告实际出现的构件，不再给模型完整设施构件词表；
- 非桥梁设施禁止生成桥面系、上部结构、下部结构等无证据桥梁术语；
- 人行天桥允许按官方/Gold习惯使用“该桥、桥梁”；
- 规范名称中的“隧道”等词不会被当作当前设施类型；
- 当前证据中不存在的止水带、防水层等构件会触发字段回退。

## 5. 保守选择而非盲目覆盖

- 人行通道等已出现稳定正增量的设施，在校验通过后允许增强；
- 对已经符合官方四段式、无内部术语且长度合理的桥梁 Baseline，保留 Baseline；
- 只有 Baseline 明显存在长文倾倒、内部术语、成因垃圾或安全影响矛盾时，才接受模型结果；
- 校验失败与“Baseline本来更好”分开记录为 `validation_fallback` 和 `baseline_quality_gate`。

## 6. 取消第二次整段生成

第一次结果存在语义问题时直接字段级回退，不再携带同等规模上下文重新请求一次。该调整避免重复消耗，并减少第二次生成继续放大错误的概率。

## 验证边界

- 专项测试：67 passed；
- `compileall`：通过；
- 本 Review 包缺少 API 凭据、原始 DOCX 和完整向量矩阵，因此没有伪造新的 4/16 份真实模型分数；
- 完整测试集中依赖生产模板、schema、CLI 包和人行通道知识卡的测试无法由该 Review 包独立执行，与本次修改无关。
